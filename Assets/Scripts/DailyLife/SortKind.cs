namespace CreatureExperiment.DailyLife
{
    /// <summary>The two visually-distinct item classes Daily-Life 0.1 sorts. Nothing more - no data, no weight, no meaning.</summary>
    public enum SortKind
    {
        Cube,
        Sphere
    }
}
